"""Top-level package for projmem.

This package exposes the implementation behind the `pmem` command. The package
name is `projmem` for distribution, but the import namespace is `pmem` so that
the code mirrors the CLI and the product language used in the roadmap.
"""

__all__ = ["__version__"]

__version__ = "0.4.0a1"
