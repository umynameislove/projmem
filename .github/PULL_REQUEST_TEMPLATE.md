## Summary
What does this PR change and why?

## Checklist
- [ ] Tests added/updated and `pytest` passes (coverage stays ≥ 95%)
- [ ] `ruff check .` and `ruff format --check .` pass
- [ ] `pyright` passes
- [ ] No raw text (failure/decision/note/command/config values) added to public output surfaces
- [ ] CHANGELOG.md updated if user-facing

## Notes
Anything reviewers should know (design trade-offs, follow-ups).
